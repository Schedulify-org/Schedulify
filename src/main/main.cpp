#include "main/main.h"

int main() {
    string inputPath = "../data/V1.0CourseDB.txt";
    string modifiedOutputPath = "../data/V1.schedOutput.txt";

    main_app(inputPath, modifiedOutputPath);

    return 1;
}